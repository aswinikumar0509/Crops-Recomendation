// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {

    repositories {
        google()
        jcenter()

    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.6.2'


        // usb debugging must galti se deny ho gya
        // reconnect the phone
        // is it ok
        //ok but mai usb nikaal dunga tab bhi chalega na??
        // yes ok
        // now complete the other payment after you have checked the app
        //dont worry i will give you full payment ok
        // shoulld i close the teamviewer now
        //ok


        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()

    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
